# 블러 에디터 — 기획 정리

## 한 줄 정의
사진·동영상에 블러 처리와 간단한 편집(손글씨 텍스트, 크롭)을 해서
인스타/스레드 업로드용으로 빠르게 내보내는 개인용 웹앱(PWA).

## 목적 / 배경
- 인스타·스레드에 사진을 올리기 전, 얼굴·정보 등을 블러 처리하고 간단히 꾸미기 위함
- 사용 기기: 안드로이드, 맥, 아이패드 — 세 기기 모두에서 문제없이 써야 함
- iOS는 개인이 앱을 사이드로딩(친구 공유)할 수 없어 네이티브 앱 배포가 막힘
- → **PWA 웹앱**으로 결정: 링크 하나로 기기·친구 공유 모두 해결, 동기화 불필요 (기기별 독립 작업)

## 플랫폼 결정
| 항목 | 결정 |
|---|---|
| 형태 | PWA (Progressive Web App) |
| 대상 기기 | 안드로이드, macOS, iPadOS — 브라우저로 접속 |
| 동기화 | 없음. 기기별 로컬 저장, 독립적으로 작동 |
| 배포 | URL 공유만으로 충분 (스토어 등록·심사 불필요) |
| 오프라인 | 편집 기능은 오프라인 동작, 업로드 시에만 인터넷 필요 |

## 핵심 기능 (우선순위 순)

### 1. 블러 처리 (가장 중요)
- 사진: 손가락으로 영역을 그려서 자유 곡선 블러
- 동영상: **객체 추적 없이** 고정 위치 블러
  - 특정 프레임에서 블러 영역(원형/사각형) 지정
  - 하단 시간 슬라이더에서 적용 구간(몇 초~몇 초) 지정
  - 해당 구간 동안 같은 좌표·모양으로 블러 고정 적용
  - 저장 시 실제 픽셀에 인코딩(원본 파일에 반영) — 화면 오버레이만이 아님

### 2. 손글씨 스타일 텍스트
- 손으로 직접 쓰는 게 아니라, **텍스트 입력 → 손글씨 폰트로 렌더링**
- 폰트는 직접 제작 불필요 — 구글 폰트(무료, 상업 이용 가능) 활용
  - 한글: 나눔손글씨 시리즈 등 2~3종
  - 영문: Caveat, Kalam 등 2~3종
- 크기, 색상, 회전 정도 조절 가능

### 3. 간단한 편집
- 크롭 (자유 비율 + 인스타/스레드용 프리셋: 1:1, 4:5, 9:16)
- 회전

## 화면 구성 (4단계)

```
[홈 · 작업 목록]
   │  ＋ 새 작업
   ▼
[파일 선택]  — 촬영 or 사진첩에서 불러오기
   │
   ▼
[편집 화면]  — 캔버스 + 하단 도구(블러 / 손글씨 / 크롭)
   │  (동영상은 시간 슬라이더 추가)
   │  완료
   ▼
[내보내기]  — 사진첩 저장 / 바로 공유(카톡·인스타 등) / 목록으로
```

- **홈**: 작업 중 / 완료 항목을 썸네일로 나열. 항목 삭제 가능(관리 모드)
- **뒤로가기 = 자동 저장**: 편집 중 어디서든 나가도 진행 상태가 로컬에 저장됨
- 완료 후에도 원본 작업은 목록에 남고, 필요 없으면 사용자가 직접 삭제

## 데이터 저장 / 안정성
- **자동 저장**: 편집 상태(블러 위치, 텍스트, 폰트 등)를 브라우저 로컬 저장소(IndexedDB)에 주기적 기록
- 앱을 껐다 켜도 "이어서 작업" 형태로 복구됨
- 처음부터 구조에 포함할 것 (나중에 추가하려면 손이 더 많이 감)
- 작업 목록에서 완료/미완성 항목을 개별 삭제 가능 → 저장 공간 관리

## 공유 흐름
```
핸드폰 카메라로 촬영
   → PWA 웹앱에서 불러오기
   → 블러 / 손글씨 / 크롭 편집
   → 로컬(사진첩)에 저장
   → 인스타 업로드 or 카톡 공유 (Web Share API 지원 시 앱 내에서 바로 공유 가능)
```

## 난이도 참고 (개발 시)
| 기능 | 난이도 |
|---|---|
| 사진 블러(영역 선택/자유곡선), 크롭, 텍스트 삽입 | 쉬움 |
| 손글씨 폰트 적용 | 쉬움 |
| 동영상 고정 위치 블러(추적 없음) | 중간~쉬움 |
| 자동 저장/복구(IndexedDB) | 중간 |
| 동영상 실제 인코딩(블러를 픽셀에 굽기) | 중간 — 처리 시간 소요 |
| ~~동영상 객체 자동 추적 블러~~ | 어려움 — **범위 제외** |
| ~~실제 손글씨(필기 인식/압력 감지)~~ | — **해당 없음(텍스트 입력 방식으로 대체)** |

## 범위에서 제외한 것
- 동영상에서 얼굴 등을 자동 인식·추적하며 따라다니는 블러 (대신 고정 위치 + 구간 지정 방식 채택)
- 손으로 직접 쓰는 손글씨 (대신 텍스트 입력 + 손글씨 폰트로 대체)
- 여러 기기 간 동기화 (기기별 독립 작업으로 충분)
- 동영상 트리밍/클립 합치기 등 별도 동영상 편집 기능

## 참고: 화면 흐름 프로토타입
클릭 가능한 목업으로 홈 → 파일 선택 → 편집 → 내보내기 흐름 확인 완료.
(도구 전환, 하위 패널, 자동 저장 표시, 목록 관리까지 시각적으로 검증됨)
